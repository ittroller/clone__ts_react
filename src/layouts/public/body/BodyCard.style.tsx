import { Layout } from 'antd';
import styled from 'styled-components';

export const BodyCardStyle = styled(Layout)`
  .body-content {
    margin: 30px;
    box-shadow: 3px 3px 2px 3px lightblue;
  }

  .body-footer {
    text-align: center;
  }
`;
